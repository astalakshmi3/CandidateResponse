spring:
  application:
    name: CandidateResponse
  ai:
   retry:
     max-attempts: 1

   openai:
     api-key: ${OPENAI_API_KEY}
     # base-url: https://api.openai.com/
     chat:
      options:
        model: gpt-4.o-mini
        max-tokens: 1500
        temperature: 0.7